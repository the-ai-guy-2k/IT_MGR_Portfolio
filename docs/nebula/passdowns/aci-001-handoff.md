# Passdown — ACI-001 V1 Handoff

Professional Portfolio Website V1 is a static site in `public/`.

To run locally:

```text
python -m http.server 8080 --directory public
```

To host: import or pull this repository into Replit and publish `public/` as a static deployment. Account, domain, and publish actions are Operator-controlled.

Do not invent metrics, team sizes, budgets, dates, or testimonials. Portfolio V3 remains the content baseline.

Open Operator handoff items:

- Git remote URL / repository name
- Replit project / import target
- Whether this site replaces the existing public DevOps portfolio URL
